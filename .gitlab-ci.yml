---

variables:
  CI_PYTHON_3: "yes"
  CI_PYTHON_2_VENV: "yes"

include:
  - project: 'dpres/gitlab-ci-pipeline'
    file:
      - '/pipeline/common.yml'
      - '/pipeline/fddps.yml'
      - '/pipeline/kppas.yml'
      - '/pipeline/sapa.yml'
